# Legacy Cleanup

Do not delete user data without explicit authorization.

Legacy items to archive after new base is validated:

- root logcat dumps;
- UI dumps;
- build logs;
- `update_db.cjs`;
- `update_db.py`;
- duplicate generated icons;
- obsolete docs that contradict specs.

Keep:

- legacy source for migration reference;
- known bug logs;
- previous QA reports;
- user vault databases/backups.
